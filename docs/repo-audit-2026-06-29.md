# XDownloader — Repository Audit / 代碼庫審計報告

**Date / 日期:** 2026-06-29  
**Method / 方法:** 6-dimension multi-agent audit (git-hygiene, cookie-feature, file-hygiene, docs-drift, tests-ci, code-smells), each finding adversarially re-verified against the live repo. 41 findings confirmed; 2 verify batches partially dropped (noted in TODO).

> 用途：本次「太亂了，有什麼該做沒做」的全面審計快照，供日後複盤。對應的待辦與後續整理見 `TODO.md`。

---

## Bottom line / 核心結論

The mess is process, not broken code: the security-scoped cookie-file feature is fully wired end-to-end (UI -> DownloadManager.swift:354/507 -> both services -> `--cookies <path>`) and compiles clean — it is NOT dead code. The real problems are git-hygiene: the entire feature was committed directly to main (5 unpushed commits b43ca12..4091c6d) with 4 more files left uncommitted, AND duplicated on branch fix/cookies-scope-grant (commit f1f3576) in an ephemeral /private/tmp worktree, violating your own branch + worktree conventions. Nothing is lost and origin/main is still a clean ancestor, so this is 100% reversible right now — but only until you push. Secondary gaps: zero tests for ~3,500 lines, and docs (FEATURES.md/ROADMAP.md) still say v1.0 while the app is v1.4.2.

**繁中摘要：** 亂的是 git 流程而非程式碼。進行中的 cookie 檔案安全範圍功能**已完整接通、可編譯通過、不是死碼**。真正問題：同一功能同時散落在 (1) 直接提交到 main 的 5 個未推送 commit、(2) main 工作區 4 個未提交檔案、(3) `fix/cookies-scope-grant` 上的重複 commit `f1f3576`（在 /private/tmp 臨時 worktree），違反「功能分支＋worktree、不直接動 main」的自訂規則。未推送前 100% 可救回。次要：~3,500 行零測試；FEATURES.md/ROADMAP.md 仍寫 v1.0（實際 v1.4.2）。

---

## Prioritized actions / 優先級行動清單

### P0 · (medium) Consolidate the cookie feature onto ONE branch and get it off main — it works, but it must not land on main directly (act before you push)

**Why / 為什麼:** DECISIVE ANSWER to your core worry: the feature is NOT dead code and NOT unwired. It is fully threaded (DownloadManager.swift:354 and :507 pass cookiesFile into YtDlpService/GalleryDlService; CookieArgs.make at DownloadEnums.swift:125-129 emits `--cookies <path>`; UI at SettingsView.swift:64-74) and the orchestrator confirmed `swift build` succeeds. The actual mess is that this one feature exists in THREE places at once: 5 commits committed straight to main (origin/main..main = b43ca12..4091c6d, all unpushed), 4 uncommitted working-tree files (DownloadEnums/GalleryDlService/YtDlpService/DownloadManager), AND a duplicate commit f1f3576 on fix/cookies-scope-grant whose merge-base is main's own HEAD (4091c6d). This violates BOTH standing conventions (feature work on a dedicated branch off main; all changes through a worktree). The dirty tree also pins main in place — `git checkout <branch>` errors. It is fully reversible only while unpushed; once pushed to shared main, converting to a PR needs a force-push of the default branch.

**How / 怎麼做:** The working tree strictly supersedes f1f3576 (3 of 4 files differ only in comments; GalleryDlService.swift:77 has the better NSFW error string vs f1f3576's outdated 'try a different cookie browser'). So pick the working tree as canonical and DON'T also merge f1f3576. Steps: (1) commit the 4 uncommitted files onto fix/cookies-scope-grant in the worktree (or stash, switch, pop) so the branch holds the superior version; the 5 cookie commits are already its ancestors. (2) `git checkout main && git reset --hard origin/main` (fe26ed0) — loss-free, the branch carries 4091c6d. (3) Open a PR from fix/cookies-scope-grant -> main. (4) Do this BEFORE any `git push`. Going forward, branch+worktree before the first feature commit.

### P1 · (medium) Fix the cookie security-scope lifecycle correctness + clarity issues before the PR lands

**Why / 為什麼:** These live in the same in-flight files, so fix them while the diff is still open. One is a real (if rare) bug: setCookiesFile's catch branch (DownloadManager.swift:238-243) sets a new path but never clears grantedCookiesURL, so a later download can hand the OLD file's security grant to withScope while --cookies points at the NEW path — in a sandbox the new read is ungranted. The rest is confusion debt that makes the feature hard to reason about: beginAccessingCookiesFile()'s docstring (DownloadManager.swift:215-216) says it 'starts access' but the body explicitly does NOT (line 222 'Do NOT startAccessing here'); the same file is tracked by 5 overlapping state vars (lines 13-17); three methods (stopAccessingCookiesFile DM:258-265, clearCookiesFile DM:248-250, deinit DM:68-76) call stopAccessing on freshly-resolved URLs that were never started — unbalanced no-ops; and loadSettings resolves the bookmark twice (DM:303-305 then :319).

**How / 怎麼做:** (1) In the catch branch (DM:238-243) add `grantedCookiesURL = nil` (mirror clearCookiesFile). (2) Rename beginAccessingCookiesFile() -> resolveCookiesFilePath() and rewrite the docstring to its true contract (resolves bookmark, returns path, does NOT start access). (3) Collapse the 5 state vars to 2 (keep cookiesFileBookmarkData + cookieScope; drop _bookmarkForDeinit and grantedCookiesURL as long-lived state); delete the unbalanced no-op stops (DM:258-265, :248-250, :68-76) since per-download withScope already guarantees paired start/stop. (4) Remove the redundant second resolve at DM:319 or :303-305. (5) Fix the garbled mid-sentence comment at GalleryDlService.swift:69-70 and the duplicated cookies.txt sentence at DownloadManager.swift:373-374.

### P1 · (medium) Add a test target and seed 4-6 unit tests on the pure, regression-prone logic that is being refactored right now

**Why / 為什麼:** Package.swift declares only an executableTarget, no testTarget; there are zero test files for ~3,500 lines (grep for XCTest/@testable returns nothing). That is acceptable for a hobby GUI app in steady state, but you have active, duplicated refactoring across main + the worktree on exactly the kind of pure logic a couple of tests would pin — and yt-dlp's format selector even carries a known-bug workaround (YtDlpService.swift:21-22) that begs for a regression test. CI gates compilation but never runs tests (.github/workflows/build.yml has no `swift test`).

**How / 怎麼做:** Add `.testTarget(name: "XDownloaderTests", dependencies: ["XDownloader"], path: "Tests/XDownloaderTests")` to Package.swift. Seed tests over the already-internal pure statics first (zero refactor): CookieArgs.make (DownloadEnums.swift:125 — assert file path overrides browser, .none yields []) and YtDlpService.buildArguments (YtDlpService.swift:7 — snapshot the 4 format-selector branches + subtitle gate). Then widen `private`->`internal static` to cover stripTrackingParams (DownloadManager.swift:700) and FxTwitterService.sanitize (FxTwitterService.swift:125). Finally add a `swift test` step to build.yml after the build steps.

### P1 · (small) Update FEATURES.md — your declared 'canonical' feature list still says v1.0 and omits two shipped features

**Why / 為什麼:** README.md:168 markets FEATURES.md as 'the canonical list,' yet FEATURES.md:1 still reads 'v1.0' (app is v1.4.2: Resources/Info.plist:16 + tag v1.4.2) and has ZERO mention of two clearly-shipped features: Pause/Resume/Stop (DownloadItem.swift:46 `case paused`, DownloadRowView.swift Stop/Resume buttons, merged PR #1) and SQLite cross-session history + dedup (HistoryStore.swift CREATE TABLE download_history, DownloadManager.swift:105). A canonical doc that omits shipped features actively misleads.

**How / 怎麼做:** Change FEATURES.md:1 title to v1.4.2 (or drop the hardcoded version to stop the drift recurring). Add a 'Download Control' section (Stop/Pause/Resume) and a 'History & Deduplication' section (SQLite store + duplicate-URL prompt). Re-run `grep -niE 'history|pause|resume|sqlite|dedup' FEATURES.md` to confirm.

### P2 · (trivial) Delete the two stale upstream-gone branches and tear down the /private/tmp worktree

**Why / 為什麼:** Both branches are dead clutter whose work already shipped: docs/roadmap-cleanup (2208385) removes roadmap lines already absent from main; fix/ytdlp-format-fallback (a2df4d1, 3 commits) — its `bv*+ba/b` catch-all already landed via PR #4 (YtDlpService.swift:44,51,57,61) and it is MISSING main's subtitle-429 + SiteRegistry refactor, so it carries no unique work. Both show `[origin/...: gone]`. The fix/cookies-scope-grant worktree also lives on /private/tmp, which macOS wipes on reboot (orphaning the registration) — finish using it for the PR first, then remove.

**How / 怎麼做:** After the P0 PR merges: `git branch -D docs/roadmap-cleanup fix/ytdlp-format-fallback fix/cookies-scope-grant`. Then `git worktree remove --force /private/tmp/xdownloader-scope-fix` (force needed: untracked .build dir) and `git worktree prune`. If you want a persistent worktree, recreate it under a non-tmp path.

### P2 · (small) Remove throwaway/dead artifacts: a committed verification script with a foreign machine path, two redundant icon generators, and a duplicate icon binary

**Why / 為什麼:** scripts/verify-cookies-plan.sh is TRACKED (committed in 73dd2a6/4091c6d) yet line 2 hardcodes another agent's machine path (/var/folders/38/.../grok-goal-...), is self-evidently machine-generated (duplicated '=== 3/4' headers, two 'verify script completed' markers at lines 110 & 145), and is referenced by nothing. make_icon.py + make_icon.swift are two redundant dead generators (both gitignored, the icon is already in Resources/AppIcon.icns). Resources/AppIcon.png is a byte-identical 832KB duplicate of the iconset's 512@2x member (same md5). Plus inert litter: empty build/, .DS_Store.

**How / 怎麼做:** `git rm scripts/verify-cookies-plan.sh` and commit (on the feature branch, not main). `rm make_icon.py make_icon.swift` and drop .gitignore:24-25 (keep one if you want a regenerator — prefer the .swift one). Delete Resources/AppIcon.png and point README.md:3 at the iconset member, or generate a small badge. `rm -rf build .DS_Store`. Optionally add `.omx/` to the committed .gitignore (currently only in local .git/info/exclude).

### P2 · (small) Reconcile documentation drift: stale versions, contradictory roadmaps, no CHANGELOG, stale copyright

**Why / 為什麼:** Multiple low-impact-but-misleading doc gaps: README.md:18/81 hardcode v1.3.0 (latest is v1.4.2); README.md:179 lists 'Pause / cancel' as a future roadmap item though it shipped (PR #1); README.md:191 calls ROADMAP.md the 'source of truth' yet the two roadmaps disagree (README has 'Pause/cancel', ROADMAP has 'AutoRaise'); ROADMAP.md:3 still frames the project as 'v1.0 — Shipped' across 8 later releases; no CHANGELOG exists despite 9 tags; Info.plist:28 copyright still © 2025.

**How / 怎麼做:** Drop the explicit version from README.md:18/81 (the badge at line 13 is already dynamic). Delete README.md:179. Collapse README's duplicated roadmap (README.md:177-189) to just the ROADMAP.md pointer to kill the divergence. Update ROADMAP.md:3 to 'Shipped (current: v1.4.2)'. Add a CHANGELOG.md (Keep a Changelog format) back-filled from the 9 tag-aligned commit subjects. Bump Info.plist:28 to '2025-2026'.

### P3 · (large) Tidy code smells opportunistically — bundle into the cookie PR or do as separate behavior-preserving commits

**Why / 為什麼:** Not urgent (the app works), but these reduce future mess: DownloadManager.swift is a 716-line god object mixing 7 concerns; media-extension literal lists are duplicated across 6 sites with DIVERGENT contents (YtDlpService.swift:111 is missing gif/avif; GalleryDlService knownMedia at :140 omits mkv/m4v its own isVideo at :144 then tests — a dead branch); the mediaCategory derivation is copy-pasted 4x; GalleryDlService.cookieArgs (:207-209) is now a redundant pass-through wrapper; title-stem stripping diverges between YtDlpService.swift:124-143 and GalleryDlService.swift:197-205; _bookmarkForDeinit (DM:15) is a non-idiomatic redundant mirror.

**How / 怎麼做:** Highest value: extract cookie/bookmark lifecycle into a CookieAccessManager type (kills the scatter the P1 fix touches) and JSON queue persistence into a QueueStore mirroring HistoryStore. Add a shared `enum MediaExtensions { image/video/audio/all }` and replace the 6 literal lists (auto-fixes the gif/avif and mkv/m4v divergences). Add `DownloadItem.recomputeMediaCategory()` for the 4 dupes. Inline CookieArgs.make at GalleryDlService.swift:19 and delete the :207-209 wrapper. Do NOT extract the download/fallback orchestration (DM:322-534) — that is the manager's legitimate core.

### P3 · (medium) Close the single-source-version TODO and harden CI/release

**Why / 為什麼:** Your standing TODO (single-source version from the git tag) is half-done: build.sh:33-36 stamps the version from `git describe` only into the BUILT bundle, but Resources/Info.plist:14,16 still carries a hand-edited '1.4.2' that any `swift run` path uses and that must be manually bumped each release. CI also never exercises build.sh/the .app bundle until a tag is pushed, runs no lint/format, and release.yml:3's example uses a 2-segment tag (`v1.1`), contradicting your 3-segment SemVer convention.

**How / 怎麼做:** Replace Info.plist:14/16 with a sentinel (0.0.0) and make build.sh:26 hard-fail when `git describe` finds no tag, so the tag is the sole source. Add a non-publishing bundle-smoke step to build.yml (run `./build.sh release` with `fetch-depth: 0` so stamping is exercised, assert the binary + CFBundle keys exist). Add a `swift format lint --strict --recursive Sources` step (Xcode 16 bundles swift-format). Update release.yml:3 to a 3-segment example and optionally tighten the glob to `v[0-9]+.[0-9]+.[0-9]+`.

---

## All 41 verified findings (by dimension & severity) / 全部已驗證發現

### Dimension: git-hygiene (7)

- **[HIGH] Entire security-scoped cookie-bookmark feature (5 commits) committed DIRECTLY to main with no branch and no PR — violates the dedicated-branch convention**
  - *Category:* convention-violation
  - *Evidence:* git log origin/main..main shows 5 unpushed commits all on main: b43ca12, 73dd2a6, e09dbbe, e1dfb61, 4091c6d (the 'security-scoped cookie bookmark' feature). Memory convention: 'Feature work MUST go on a dedicated branch off main, NOT committed directly to main' and 'all code changes MUST go through a git worktree, never directly on the working tree'. These were authored on the main working tree, not a worktree/feature branch.
  - *Recommendation:* Confirmed process violation, still fully local and reversible (origin/main is 5 commits behind, nothing pushed). Note the recovery is easier than the prior agent suggested: the existing worktree branch fix/cookies-scope-grant already contains ALL 5 of these commits plus its own f1f3576, so the feature history is already preserved on a proper branch — no new `git branch feature/cookie-scope` is needed. Concrete steps: (1) First reconcile the 4 uncommitted main edits (DownloadEnums/GalleryDlService/YtDlpService/DownloadManager) against f1f3576 on fix/cookies-scope-grant, which appears to duplicate the same CookieArgs/cookiesFile work — stash them (`git stash`) and diff against the branch; fold in anything not already there, discard the rest. (2) Restore main to its pushed state: `git checkout main && git reset --hard origin/main`. (3) Open the PR from fix/cookies-scope-grant -> main so the cookie feature gets review before landing. Going forward, branch (and use a worktree) before the first feature commit. Keep severity high: it is an entire 5-commit feature committed straight to main in violation of two explicit standing conventions, and main currently diverges from origin so the violation compounds until corrected — but it is non-destructive to fix.
- **[MEDIUM] Cookie work is duplicated AND functionally diverged across main (uncommitted) and branch fix/cookies-scope-grant (commit f1f3576)**
  - *Category:* duplicate-work
  - *Evidence:* git merge-base f1f3576 main = 4091c6d (main's HEAD): f1f3576 is exactly one commit on top of main's tip, re-implementing the same CookieArgs/cookiesFile threading that sits UNCOMMITTED on main (same 4 files: DownloadEnums, GalleryDlService, YtDlpService, DownloadManager). Per-file comparison `diff <(git show f1f3576:FILE) <(cat FILE)`: YtDlpService.swift = IDENTICAL; DownloadEnums.swift + DownloadManager.swift differ in COMMENTS only; GalleryDlService.swift differs FUNCTIONALLY — main's uncommitted tree has the improved user string `"No media found — the tweet may be deleted, or export cookies.txt in Settings (recommended for X sensitive/NSFW content)."` (GalleryDlService.swift:77) plus an extra comment line (line 69-70), which f1f3576 does NOT contain (its blob still says `...try a different cookie browser in Settings.`). Diffstat confirms divergence: f1f3576 touches GalleryDlService with 7 changed lines, main's uncommitted diff with 12.
  - *Recommendation:* Keep ONE canonical copy: main's uncommitted working tree. It is functionally identical to f1f3576's cookie logic (the CookieArgs.make refactor + cookiesFile threading are byte-identical in 3 of 4 files) EXCEPT it has the superior NSFW error string at GalleryDlService.swift:77 and an extra clarifying comment — so it strictly supersedes f1f3576. Do NOT both commit main's changes AND merge f1f3576; that double-applies the refactor and conflicts on the GalleryDlService string. Concrete steps: (1) Move main's uncommitted 4-file change off main onto a proper feature branch built on top of the 5 cookie commits (it should not be sitting uncommitted on main per the feature-branch + worktree conventions). (2) Delete branch fix/cookies-scope-grant and remove its worktree at /private/tmp/xdownloader-scope-fix (git worktree remove), since f1f3576 contributes nothing beyond stale comments and an outdated error string. Severity is medium, not high: this is duplicate in-progress work with a small, trivially-resolved one-string merge-conflict risk — not dead code or a broken/unwired feature, and each copy works on its own.
- **[MEDIUM] The 5 cookie commits are unpushed and origin/main is a clean ancestor — they can be moved to a branch/PR cleanly right now (act before pushing)**
  - *Category:* branch-hygiene
  - *Evidence:* git rev-parse origin/main = fe26ed0 (parent of b43ca12); `git merge-base --is-ancestor origin/main main` returns YES (exit 0). `git rev-list --left-right --count origin/main...main` = 0 behind / 5 ahead. Nothing pushed yet, no divergence — a clean fast-forward boundary.
  - *Recommendation:* Relocate the 5 cookie commits off main now, while they are still local and origin/main (fe26ed0) is an unmodified ancestor — this is a clean, rewrite-free window. Concrete steps: (1) stash or commit the 4 uncommitted working-tree files first so they don't get caught up: `git stash` (or commit them onto the same feature branch). (2) Create the feature branch at current HEAD: `git branch fix/cookies-scope-bookmark 4091c6d`. (3) Reset main back to origin: `git reset --hard origin/main` (fe26ed0). (4) `git stash pop` if you stashed, onto the feature branch, and open a PR from fix/cookies-scope-bookmark. Before doing this, reconcile with the existing worktree branch fix/cookies-scope-grant (commit f1f3576), which appears to duplicate this same cookie/CookieArgs work — pick one branch as the source of truth to avoid landing the feature twice. Act before any `git push` to origin/main: once these 5 commits are pushed directly to the shared main, converting them to a PR requires a force-push / history rewrite of the default branch, which is the painful outcome this finding warns against. Severity stays medium: it is a genuine process-convention violation on real feature code (not dead code, not cosmetic), but it is currently 100% cleanly reversible with no data loss.
- **[MEDIUM] Dirty main working tree blocks switching to any other branch and pins main in place**
  - *Category:* workflow
  - *Evidence:* git status: 4 modified files (DownloadEnums.swift, GalleryDlService.swift, YtDlpService.swift, DownloadManager.swift), none staged. All 4 also differ on fix/ytdlp-format-fallback and docs/roadmap-cleanup (and 3 of 4 differ on fix/cookies-scope-grant), so `git checkout <branch>` would error 'local changes would be overwritten'. The uncommitted DownloadManager.swift edit depends on CookieFileScope.withScope/grantedCookiesURL introduced by the committed cookie commits, so it cannot simply be dropped without losing the NSFW error-string improvement.
  - *Recommendation:* The dirty tree genuinely blocks branch switching and is a real workflow/convention violation (the whole cookie feature sits on main as 5 unpushed commits + 4 uncommitted files, against the "feature branch + worktree, never on main" rules) — keep at medium, not higher, because almost nothing is at risk of loss: the worktree branch fix/cookies-scope-grant already contains all 5 main commits PLUS f1f3576, the committed equivalent of the uncommitted refactor. Concrete fix: (1) Capture only the unique delta — the NSFW error string + NSFW comments in GalleryDlService.swift and DownloadEnums.swift — onto fix/cookies-scope-grant in the worktree at /private/tmp/xdownloader-scope-fix (a few lines; do NOT re-commit the whole CookieArgs refactor, it is already there as f1f3576). (2) On main, clear the working tree (`git stash` or `git checkout -- .`) so it matches HEAD, then move the prematurely-committed feature off main with `git reset --hard origin/main` — this is loss-free because fix/cookies-scope-grant already carries 4091c6d as an ancestor. (3) Land the consolidated feature via a PR from fix/cookies-scope-grant, restoring main to origin/main and honoring the branch/worktree conventions. Do NOT follow the original "commit the 4 files to a new feature branch preserving the GalleryDlService improvement" verbatim — that would fork a third near-identical copy of the CookieArgs work.
- **[LOW] Stale branch fix/ytdlp-format-fallback is NOT useful unmerged work — its core fix already shipped in main, and merging it would REVERT main's SiteRegistry refactor**
  - *Category:* stale-branch
  - *Evidence:* Branch tip a2df4d1, 3 commits, merge-base ace3c11 = 15 commits behind main. Its headline commit 69428d7 'harden yt-dlp format selector with bv*+ba/b catch-all' duplicates 9b9086a (#4) ALREADY in main — the catch-all `bv*+ba/b` is present at YtDlpService.swift:44,51,57,61. `git diff main fix/ytdlp-format-fallback` shows merging it would UNDO main's refactor: revert SiteRegistry.profile(for:) back to SiteKind(url:), strip the subtitle-429 non-fatal handling (item.subtitleDownloadFailed), and remove the .f136 suffix-strip logic. The other two commits net to the 'click Retry' wording that main already has (YtDlpService.swift:199). Upstream is `[origin/fix/ytdlp-format-fallback: gone]`.
  - *Recommendation:* Delete the local branch: `git branch -D fix/ytdlp-format-fallback` (its remote is already `gone`). Justify it accurately by superseded content, NOT by 'merge would revert the refactor' (that reasoning is diff-vs-merge confusion). The precise reason: all three commits' real payload — the `bv*+ba/b` catch-all selector, its inline comment, and the 'No downloadable format — … click Retry.' wording — already landed in main via PR #4 (9b9086a) and follow-on commits (verifiable at YtDlpService.swift lines 46/53/59/63, 198, 201). The branch carries no unique work, is missing main's subtitle-429 non-fatal handling, .f136 suffix-strip, and SiteRegistry refactor, and its upstream is gone, so nothing is lost by deletion. Severity is low: this is repo hygiene (a fully-superseded stale branch), not a code defect or dead-but-shipped feature.
- **[LOW] Stale branch docs/roadmap-cleanup is redundant — its change is already reflected in main**
  - *Category:* stale-branch
  - *Evidence:* Single commit 2208385, merge-base ace3c11 (15 commits behind main). It removes 'Persist download history' and 'Pause / cancel' lines from ROADMAP.md, but `git grep 'Persist download history' main -- ROADMAP.md` returns nothing and main's ROADMAP.md v1.x section (lines 11-12) already shows only 'Finish notification' and 'Batch input' — the post-cleanup state. Upstream is `[origin/docs/roadmap-cleanup: gone]`.
  - *Recommendation:* Delete the branch — it is purely redundant, upstream-gone local clutter whose sole change (removing two shipped roadmap lines) is already present in main. Because commit 2208385 is not an ancestor of main, `git branch -d` will refuse; use force: `git branch -D docs/roadmap-cleanup`. No content is lost (verified: only ROADMAP.md changed, 2 deletions, both already absent from main, and main's ROADMAP is strictly a superset of the branch's). The same applies to the other upstream-gone stale branch fix/ytdlp-format-fallback, but that one carries 3 format-selector commits and must be checked for unmerged code before deletion — unlike this docs-only branch which is safe to drop immediately.
- **[LOW] Second worktree lives on an ephemeral /private/tmp path and holds the duplicate (not canonical) implementation**
  - *Category:* worktree
  - *Evidence:* git worktree list: /private/tmp/xdownloader-scope-fix -> fix/cookies-scope-grant (f1f3576). Its `git status --porcelain` is clean (commit captures all state, so the f1f3576 object is safe in the shared object store), but /private/tmp is macOS scratch space cleared on reboot, which would orphan the worktree registration (requiring `git worktree prune`). Ironically the memory convention says code changes must go through a worktree, yet the shipped cookie feature was done on main directly while this worktree holds only the competing/older duplicate.
  - *Recommendation:* Keep severity low: the commit f1f3576 and branch ref fix/cookies-scope-grant live in the shared .git object store/refs, so a /private/tmp wipe loses no committed work — it only orphans the worktree registration (fix with `git worktree prune`). This is housekeeping, not dead code. BUT do not remove blindly: f1f3576 diverges from main's uncommitted cookie refactor in 3 of 4 files, so first reconcile. Steps: (1) diff the two variants per file — `for f in Sources/XDownloader/Models/DownloadEnums.swift Sources/XDownloader/Services/GalleryDlService.swift Sources/XDownloader/ViewModels/DownloadManager.swift; do diff <(git show f1f3576:$f) $f; done` — and pick the canonical version. (2) Land the chosen version on a proper feature branch off main (per the worktree/feature-branch conventions). (3) Then `git worktree remove --force /private/tmp/xdownloader-scope-fix` (force needed because the worktree contains an untracked .build dir) and delete the redundant branch. (4) If you want to keep working in a worktree, relocate it to a persistent path with `git worktree move` instead of leaving it under /private/tmp; run `git worktree prune` if the tmp dir was already cleared.

### Dimension: cookie-feature (4)

- **[LOW] Feature is FULLY WIRED end-to-end and compiles — it is NOT dead code**
  - *Category:* verification
  - *Evidence:* The new `cookiesFile` param is actually passed at BOTH call sites: DownloadManager.swift:354 `cookiesFile: fileToPass` into YtDlpService.buildArguments, and DownloadManager.swift:507 `cookiesFile: fileToPass` into GalleryDlService.run. `fileToPass = beginAccessingCookiesFile()` (DM:342, DM:500). UI is reachable: SettingsView.swift:67-76 'Choose cookies.txt...' button -> manager.setCookiesFile(from:url); SettingsView.swift:63-65 'Clear' -> manager.clearCookiesFile(); path shown at SettingsView.swift:59-62. CookieArgs.make (DownloadEnums.swift:122-129) routes the file into `--cookies <path>` for both services. `swift build` in /Users/gin/Repo/my-downloader => 'Build complete! (27.60s)', no errors. So the orchestrator's worst-case hypothesis (entire feature + bookmark scaffolding is dead code) is REFUTED.
  - *Recommendation:* No code action needed for wiring or compilation — the feature is fully threaded UI -> DownloadManager -> both services -> '--cookies <path>' and builds clean. The genuine, separate issue is process, not code: this whole feature currently sits as 4 uncommitted modified files on main (DownloadEnums.swift, GalleryDlService.swift, YtDlpService.swift, DownloadManager.swift), on top of 5 unpushed commits (b43ca12..4091c6d) that were committed DIRECTLY to main. This violates the user's persistent conventions (feature work on a dedicated branch off main via a git worktree, never on main directly). Recommended: move these uncommitted changes plus the 5 main-only commits onto a feature branch / worktree (the existing fix/cookies-scope-grant worktree duplicates this same work and should be reconciled, not double-landed) and open a PR rather than continuing on main.
- **[LOW] beginAccessingCookiesFile() docstring now contradicts its own behavior after the refactor**
  - *Category:* correctness
  - *Evidence:* DownloadManager.swift:215-216 header still reads: '/// Starts (or restarts) security-scoped access for the stored bookmark and returns the usable path'. But the refactored body (DM:221-225) explicitly does the opposite: comment at DM:222 says 'Do NOT startAccessing here' and the body only resolves the bookmark + sets grantedCookiesURL, never calling startAccessingSecurityScopedResource. The function name 'beginAccessing...' and its public docstring now lie about what it does; the actual start happens only in CookieFileScope.begin (CookieFileScope.swift:17/20).
  - *Recommendation:* Rename beginAccessingCookiesFile() to something like resolveCookiesFilePath() and rewrite the docstring (DM:215-216) to state its true contract: 'Resolves the security-scoped bookmark, stores the granted URL in grantedCookiesURL, and returns the path for --cookies. Does NOT start access — startAccessing is deferred to CookieFileScope.begin for the child-process lifetime.' Keep the renamed call sites (DM:236, 319, 342, 500) intact. No functional change is needed since access is already correctly started in CookieFileScope; this is a documentation/naming accuracy fix only.
- **[LOW] setCookiesFile catch (plain-path) branch leaves grantedCookiesURL stale, can route the wrong file's grant**
  - *Category:* correctness
  - *Evidence:* setCookiesFile's catch branch (DM:238-243) sets cookiesFileBookmarkData=nil and cookiesFilePath=newPath but does NOT reset grantedCookiesURL. If a previously-bookmarked file had set grantedCookiesURL, the next download passes that stale URL into withScope (DM:344/501, grantedURL: grantedCookiesURL). CookieFileScope.begin prefers grantedURL over file (CookieFileScope.swift:15-17), so the active security grant covers the OLD file while --cookies points at the NEW path (DM passes cookiesFile:fileToPass = new cookiesFilePath) — in a sandbox the new path read would be ungranted. Rare (only when bookmarkData(options:.withSecurityScope) throws), but a real mismatch.
  - *Recommendation:* In setCookiesFile's catch branch (DownloadManager.swift:238-243), mirror clearCookiesFile's cleanup so the stale grant cannot be routed to the wrong file. Minimal fix: add `grantedCookiesURL = nil`. Complete fix (also closes the prior-grant/bookmark leak the success branch handles at line 232): `} catch { stopAccessingCookiesFile(); if let u = grantedCookiesURL { u.stopAccessingSecurityScopedResource(); grantedCookiesURL = nil }; cookiesFileBookmarkData = nil; _bookmarkForDeinit = nil; cookiesFilePath = url.path; saveSettings() }`. This guarantees that when no security-scoped bookmark exists for the current path, withScope falls into the plain-file branch (begin uses URL(fileURLWithPath: file)) instead of an out-of-date grantedURL.
- **[LOW] Garbled inline comment injected mid-sentence in GalleryDlService**
  - *Category:* code-quality
  - *Evidence:* GalleryDlService.swift:66-71: the diff jammed a how-to sentence into the middle of an unrelated explanatory comment about the `guard item.outputPath != nil` block, breaking the prose: '...the cookie session // can't unlock). Export a cookies.txt from your browser (Settings) for stubborn X adult content. // Without this guard, the row would be marked "Done"...'. The 'Export a cookies.txt...' sentence interrupts the 'can't unlock) ... Without this guard' explanation.
  - *Recommendation:* Delete the injected sentence from the comment. In GalleryDlService.swift line 69, change '// can't unlock). Export a cookies.txt from your browser (Settings) for stubborn X adult content.' followed by line 70 '// Without this guard, the row would be marked "Done"' back to a single line '// can't unlock). Without this guard, the row would be marked "Done"'. The cookies.txt guidance is already surfaced to the user in the failure status string at GalleryDlService.swift:77 and in the Settings help text at SettingsView.swift:80, so it does not belong in this internal explanatory comment. Trivial one-line cleanup; can be folded into the in-progress CookieArgs refactor before it is committed.

### Dimension: file-hygiene (5)

- **[LOW] Throwaway cookie-feature verification script committed permanently with a foreign agent's hardcoded scratch path**
  - *Category:* Dead/throwaway artifact committed to repo
  - *Evidence:* scripts/verify-cookies-plan.sh is tracked (git ls-files) and was committed in 4091c6d/73dd2a6. Line 2 hardcodes another agent's machine-local temp dir: SCRATCH=${SCRATCH:-/var/folders/38/dj7s1z852kqbpvzm3g5qmq2r0000gn/T/grok-goal-9944274945b3/implementer}. grep counts 21 refs to /tmp, /var/folders or grok-goal. The file is also machine-generated garbage: step headers '=== 3' and '=== 4' appear twice (lines 22/25 then again 112/142) and there are TWO terminal markers — line 110 'verify script completed (single-shot, exit 0)' AND line 145 'verify script completed successfully'. It writes one-off swift files to /tmp/real-verify-args.swift and /tmp/test-grant-e2e.swift. grep of .github/, build.sh, README.md shows it is referenced by nothing.
  - *Recommendation:* Confirmed real but low impact: a non-shipping, throwaway verification script with zero effect on the SwiftPM build or app runtime — pure repo hygiene, not dead production code, so 'medium' overstates it. Remove it on a branch/worktree (per the project's worktree + feature-branch conventions; it was originally committed directly to main, itself a separate process violation): `git rm scripts/verify-cookies-plan.sh` (also removes the now-empty scripts/ dir from tracking), then commit. Concrete reasons to delete: (1) line 3 hardcodes another machine's absolute path /var/folders/38/.../grok-goal-9944274945b3/implementer (won't exist elsewhere; the ${SCRATCH:-...} default would silently mkdir that foreign path on any other machine); (2) self-evidently machine-generated/appended garbage — duplicated '=== 3'/'=== 4' headers and two 'verify script completed' markers (lines 110 & 145), with lines 116-138 writing then immediately clobbering /tmp/real-verify-args.swift; (3) wired into nothing (no CI/build/README reference). If a reusable cookie check is genuinely wanted, replace it with a clean idempotent script using repo-relative paths and $(mktemp -d) scratch, referenced from .github/ CI; otherwise keep one-shot verification artifacts out of version control.
- **[LOW] Two redundant dead icon-generator scripts both present on disk (make_icon.py + make_icon.swift)**
  - *Category:* Redundant dev script
  - *Evidence:* ls shows both make_icon.py (8638 bytes) and make_icon.swift (9709 bytes) at repo root; both are gitignored (.gitignore:24-25 'make_icon.py' / 'make_icon.swift'). Both generate the same icon — header of make_icon.py: 'X Downloader – App Icon Generator ... Void Descent'; make_icon.swift: 'X Downloader – App Icon ("Void Descent")'. grep -rn make_icon over the whole tree returns only the two .gitignore lines — neither build.sh nor any workflow invokes either. The shipped icon is already committed (Resources/AppIcon.icns + AppIcon.iconset), so both generators are dead.
  - *Recommendation:* Keep at low (arguably trivial): these are gitignored, git-UNTRACKED local-only files, so they pollute only this developer's working tree, not the repo for anyone cloning, and the produced icon is already baked into committed Resources/AppIcon.icns + AppIcon.iconset. Lowest-effort correct action: delete both local scripts (`rm /Users/gin/Repo/my-downloader/make_icon.py /Users/gin/Repo/my-downloader/make_icon.swift`) since the icon is already shipped. If a regenerator is wanted for future icon tweaks, keep exactly one — prefer make_icon.swift (matches the macOS/CoreGraphics/Swift toolchain, no Python+Pillow+numpy dependency) and delete make_icon.py. Either way, remove the now-unused entry from .gitignore lines 24-25. No code or build impact; nothing references either script.
- **[LOW] Committed AppIcon.png is a byte-for-byte duplicate of the iconset's 512@2x member (832 KB)**
  - *Category:* Duplicate committed binary
  - *Evidence:* md5 of Resources/AppIcon.png == md5 of Resources/AppIcon.iconset/icon_512x512@2x.png (both f04c2a676542e797c697e643b8c0db2c), each 832K. AppIcon.png is not dead (README.md:3 uses it as the header image), but it duplicates an existing iconset file exactly. The build (build.sh:40-41) only copies AppIcon.icns into the bundle, so AppIcon.png exists solely for the README.
  - *Recommendation:* Low severity is correct: it is a harmless size/redundancy issue, not dead or broken code (README.md:3 actively uses it). Two clean fixes: (1) Dedup by deleting Resources/AppIcon.png and pointing README.md:3 at the existing iconset member (src="Resources/AppIcon.iconset/icon_512x512@2x.png"), removing 830 KB with zero new files; or better (2) since README renders it at only 128x128 while the committed file is the full 1024px master, generate a small purpose-built badge (e.g. `sips -Z 256 ...` -> Resources/readme-badge.png) and reference that, cutting the asset from ~830 KB to a few KB. Either is optional cleanup; no functional impact.
- **[LOW] Agent tooling litter (.omx/, .DS_Store, empty build/) sitting in the working tree**
  - *Category:* On-disk litter (correctly untracked)
  - *Evidence:* find/ls shows .omx/ (logs/, metrics.json, state/sessions/...), an 8196-byte .DS_Store, and an empty build/ dir in the working tree. git check-ignore confirms none are tracked: .DS_Store via .gitignore:2, build/ via .gitignore:8, XDownloader.app/ via .gitignore:21, .build via .gitignore:7. So the 'confirm not tracked' check PASSES, but the residue is still cluttering the checkout — .omx/ is orchestrator/agent state with no place in this project.
  - *Recommendation:* Safe to delete the inert litter now: `rm -rf build .DS_Store` (build/ is empty; .DS_Store is a Finder artifact, both untracked). Treat .omx/ separately: it is live orchestrator/agent state (state/session.json, current-task-baseline.json, sessions/<active-id>/...) and is ignored via .git/info/exclude:7 (not .gitignore). Removing it mid-session could disrupt the active agent run, so delete .omx/ only after the orchestration session ends. No git action is needed for any of these since none are tracked; this is cosmetic checkout hygiene, not a correctness/security issue. Optionally add `.omx/` to .gitignore if you want it covered by the project's shared ignore rather than the local-only exclude.
- **[LOW] .omx/ is ignored only via local .git/info/exclude, not the committed .gitignore**
  - *Category:* Ignore-rule fragility
  - *Evidence:* git check-ignore -v reports .omx matches '.git/info/exclude:7:.omx/' — a machine-local exclude — whereas .DS_Store/build/etc. match the committed .gitignore. The committed .gitignore contains no .omx entry. A fresh clone on another machine would not ignore an .omx/ directory.
  - *Recommendation:* Severity low is accurate — this is repo hygiene, not dead code or security. Two concrete fixes: (1) If the .omx/ tooling is expected to keep running inside this project, add a line `.omx/` to the committed `.gitignore` (alongside the existing `.claude/` entry, since both are tooling-state dirs) so the ignore is portable across clones. (2) Preferably, point that tooling to write its logs/metrics/state outside the repo (or into an already-ignored path) so no ignore rule is needed at all. Until one of these is done, the only thing preventing accidental commits of .omx/logs, .omx/metrics.json, and .omx/state is this single developer's local .git/info/exclude.

### Dimension: docs-drift (8)

- **[MEDIUM] FEATURES.md still titled "v1.0" — 4 minor releases stale, and the "canonical" list omits two shipped features**
  - *Category:* documentation-drift
  - *Evidence:* FEATURES.md:1 reads "# XDownloader v1.0 — Feature List" while the app is v1.4.2 (Resources/Info.plist:16 + git tag v1.4.2). README.md:168 calls FEATURES.md "the canonical list," yet it has ZERO mention of two features that are clearly shipped: (1) Pause/Resume/Stop — DownloadItem.swift:46 `case paused`, DownloadRowView.swift:11-12 (onPause/onResume), :195 Text("Stop"), :236 Text("Resume"), merged PR #1 `feat/filter-pause-resume`; (2) SQLite cross-session history + dedup — HistoryStore.swift:2 `import SQLite3`, :55 `CREATE TABLE download_history`, DownloadManager.swift:46,105. `grep -niE 'history|pause|resume|sqlite|dedup' FEATURES.md` returns NONE.
  - *Recommendation:* Update FEATURES.md so it matches the shipped app: (1) change the line-1 title from "# XDownloader v1.0 — Feature List" to "# XDownloader v1.4.2 — Feature List" (or drop the hardcoded version entirely to avoid recurring drift — ties into the deferred "single-source version from git tag" TODO); (2) add a "Download Control" section documenting Stop/Pause + Resume (UI in DownloadRowView.swift, state DownloadItem.swift:46 `case paused`, from merged PR #1); (3) add a "History & Deduplication" section documenting the SQLite cross-session history store (HistoryStore.swift, CREATE TABLE download_history) and the duplicate-URL detection prompt (DownloadManager.swift:105 mostRecentCompleted dedup + confirmDuplicateDownload). After editing, re-run `grep -niE 'history|pause|resume|sqlite|dedup' FEATURES.md` to confirm the sections are present. Keep severity medium: README explicitly markets this file as canonical, so the omissions actively mislead, but no code is broken.
- **[LOW] README lists "Pause / cancel an in-progress download" as a future roadmap item, but it already shipped**
  - *Category:* documentation-drift
  - *Evidence:* README.md:179 lists "Pause / cancel an in-progress download" under the Near-term roadmap ("What's next"). That feature is shipped: DownloadStatus has `.paused` (DownloadItem.swift:46), the row UI exposes Stop/Resume buttons (DownloadRowView.swift:192-204 "Stop download — you can resume later", :232-236 "Resume"), and it landed via merged PR #1 `feat/filter-pause-resume` (commit 3e28fa9).
  - *Recommendation:* Delete README.md:179 ("- Pause / cancel an in-progress download") from the Near-term roadmap — it shipped via PR #1 (commit b76bb22) and is fully wired (DownloadManager.pauseItem/resumeItem, DownloadRowView Stop/Resume, ContentView.swift:250-251). Note ROADMAP.md (README's declared source of truth) already omits it, so this removal also re-aligns README.md with ROADMAP.md. Optionally add a "Stop / Resume an in-progress download" bullet to the README Features section and to FEATURES.md so the shipped capability is documented. Leave README.md:178 "macOS notification when a download completes" — that one genuinely matches the still-pending ROADMAP.md:11 item. Severity is low: this is cosmetic documentation drift, no functional impact.
- **[LOW] README hardcodes download CTA + quick-start to v1.3.0 while latest release is v1.4.2**
  - *Category:* versioning
  - *Evidence:* README.md:18 "⬇ Download XDownloader v1.3.0" and README.md:81 "Unzip XDownloader-v1.3.0.zip" both pin v1.3.0, two releases behind the current tag v1.4.2 (`git tag -l` shows v1.4.0, v1.4.1, v1.4.2 all newer). The shields.io badge at README.md:13 is dynamic (releases/latest) and is fine; only the hand-written strings are stale.
  - *Recommendation:* Downgrade from medium to low: purely cosmetic documentation drift with zero functional impact — the line-18 hyperlink already points to releases/latest (users still get v1.4.2), and line 81 is just an example filename inside a fenced bash comment. Fix: drop the explicit version from both spots so they never drift again — change line 18 to "⬇ Download XDownloader" (the latest-release badge on line 13 already shows the version) and line 81 to "Unzip the downloaded XDownloader-*.zip". If you prefer an explicit version, bump both to v1.4.2 now. Long term, generate these strings from the git tag at release time (ties into the user's existing "automate versioning / single-source from tag" TODO).
- **[LOW] ROADMAP.md frames the project as "v1.0 — Shipped" and was never updated across v1.1–v1.4.2**
  - *Category:* documentation-drift
  - *Evidence:* ROADMAP.md:3 "## v1.0 — Shipped" with line 5 "All core features are live." There is no mention of v1.1, v1.2, v1.3.x, or v1.4.x despite 8 subsequent tagged releases (`git tag -l`: v1.0.1 … v1.4.2). The roadmap has not tracked any of the shipped minor/patch releases.
  - *Recommendation:* In ROADMAP.md, update the "## v1.0 — Shipped" header to reflect the current shipped version (v1.4.2) — e.g. "## Shipped (current: v1.4.2)". Either add a brief shipped-history list under it (v1.1 finish notification, v1.3.x Twitter/empty-success fixes, v1.4.x cookie/security-scope work, etc.) or, preferably, add a CHANGELOG.md (none exists today) and link the roadmap to it so the single "All core features are live" line stops masking the 8 post-v1.0 releases. Also move any of the "v1.x — Near-term" bullets that have actually shipped (e.g. finish notification) into the shipped section so Near-term reflects what is genuinely pending. Cosmetic only; no code impact.
- **[LOW] README claims ROADMAP.md is "the source of truth" but the two roadmaps disagree**
  - *Category:* documentation-drift
  - *Evidence:* README.md:191 "See ROADMAP.md for the source of truth," yet the lists diverge: README near-term includes "Pause / cancel" (README.md:179) which is absent from ROADMAP.md; ROADMAP.md:14 lists "Zero-click download with AutoRaise" which is absent from the README roadmap (README.md:177-181). Two roadmaps, two different contents.
  - *Recommendation:* Pick one source. Since README.md:191 already declares ROADMAP.md the source of truth, delete README's duplicated Near-term/Future lists (README.md:177-189) and collapse the Roadmap section to just the pointer line ("See ROADMAP.md for what's next"). That removes the divergence at its root rather than re-syncing two lists that will drift again. While editing, also reconcile the missing items: add "Pause / cancel an in-progress download" to ROADMAP.md (it is the only item currently only in the README) so nothing is lost when the README list is removed.
- **[LOW] No CHANGELOG despite 9 tagged releases / PRs up to #12**
  - *Category:* changelog
  - *Evidence:* `ls CHANGELOG*` → no matches. There are 9 release tags (v1.0, v1.0.1, v1.1, v1.2, v1.3.0, v1.3.1, v1.4.0, v1.4.1, v1.4.2) and merged PRs numbered through #12 (e.g. commit 8c20241 "(#12)"). Release/version history currently lives only in commit subjects and GitHub release notes.
  - *Recommendation:* Add a CHANGELOG.md at repo root in Keep a Changelog format and back-fill all 9 tagged releases. Source the entries from the existing tag-aligned commit subjects, which are already release-shaped and version-stamped — e.g. v1.4.2 → 8c20241 "per-site args in SiteProfile + Twitter large-file timeout fix (#12)"; v1.4.1 → 39d31cc "YouTube Shorts subtitle 429 fix (#9)"; v1.4.0 → b2d09d5 "recover silently-skipped tweet media + fxtwitter fallback (#8)"; v1.3.1 → 4b6fb2a "empty-success Twitter downloads (#7)". Group each under Added/Fixed/Changed and link the PR number. Going forward, append one entry per release as part of the tagging step; given the existing build.sh that auto-stamps the version from the git tag, consider a small script that appends a stub heading on tag creation to keep it from drifting like the v1.0 headers in ROADMAP.md/FEATURES.md already have.
- **[LOW] Version still hand-edited in checked-in Resources/Info.plist — single-source-from-tag TODO only half-done**
  - *Category:* versioning
  - *Evidence:* build.sh:33-36 runs PlistBuddy to stamp CFBundleShortVersionString/CFBundleVersion from `git describe --tags` (build.sh:26) but only into the BUILT bundle copy ($BUNDLE_NAME/Contents/Info.plist, copied at build.sh:19). The checked-in Resources/Info.plist:14,16 still carries a hardcoded "1.4.2" that must be hand-bumped each release (history: commit ace3c11 manually bumped it 1.0.1→1.3.0; later bumped to 1.4.2). Any non-build.sh path (e.g. `swift run`, README.md:111) uses the hardcoded source value. This matches the user's persistent TODO 'single-source version from the git tag instead of hand-editing Info.plist (deferred)'.
  - *Recommendation:* Make build.sh the single writer to close the standing TODO. Concretely: (a) In Resources/Info.plist replace the hardcoded 1.4.2 in both CFBundleVersion (line 14) and CFBundleShortVersionString (line 16) with a sentinel like 0.0.0 so a stale value can never silently ship. (b) Harden build.sh:26 — the version stamping is currently inside an `if GIT_DESC=$(git describe ...); then` that silently no-ops when the tag lookup fails, leaving the sentinel/old value in the bundle; for release builds, make a failed `git describe` (or a non-matching tag) a hard error (`exit 1`) instead of a silent skip. (c) Optionally add a lightweight pre-release/CI check that asserts the source plist (if you keep a real value) equals `git describe --tags`, warning on divergence. Lowest-effort path that fully resolves the TODO: sentinel in the source plist + fail-fast guard in build.sh so the tag is the only source of truth.
- **[LOW] Info.plist copyright year stale (© 2025, current year 2026)**
  - *Category:* documentation-drift
  - *Evidence:* Resources/Info.plist:28 NSHumanReadableCopyright "Copyright © 2025"; the current date is 2026-06-29. Cosmetic, surfaces in the About box.
  - *Recommendation:* Update the Info.plist NSHumanReadableCopyright value to a year range that includes 2026 (e.g. 2025-2026). One-line cosmetic fix; bundle into the next version bump, or fold into the deferred TODO to single-source version metadata from the git tag.

### Dimension: tests-ci (7)

- **[MEDIUM] Zero automated tests for ~3,500 lines of Swift; Package.swift has no test target**
  - *Category:* testing-gap
  - *Evidence:* Package.swift (lines 4-14) declares a single `.executableTarget` and NO `.testTarget`. `find` for *Test*.swift / *Tests*.swift under the repo returns nothing, and `grep -rniE 'XCTest|@testable|testTarget'` across all .swift/.yml returns NONE. `wc -l Sources/**/*.swift` totals 3,502 lines including services with tricky, regression-prone logic (URL tracking-param stripping, yt-dlp format-selector fallback chains, filename-stem cleanup, cookie arg building, security-scoped bookmarks).
  - *Recommendation:* Confirmed real but downgraded high->medium. The evidence is exact, but a blanket "zero tests" gap on a ~3,500-line single-developer macOS GUI app is a genuine quality/process gap, not an active defect or dead/broken feature (the bar for high/critical here). It is, however, more than cosmetic, especially given the active, regression-prone refactoring happening in parallel across main's uncommitted CookieArgs changes and the duplicated fix/cookies-scope-grant worktree — exactly where a couple of pure-unit tests would catch divergence. Concrete next step: add `.testTarget(name: "XDownloaderTests", dependencies: ["XDownloader"], path: "Tests/XDownloaderTests")` to Package.swift, create Tests/XDownloaderTests/, and seed 4-6 focused unit tests over the genuinely pure logic that is currently in-flight: URL tracking-param stripping, the yt-dlp format-selector fallback chain (YtDlpService), filename-stem cleanup, and CookieArgs argument building (DownloadEnums/GalleryDlService/YtDlpService — the four uncommitted files). Note many of these are currently inline inside services, so a small extraction into free functions or a pure helper type may be required first to make them testable without spawning Process. Treat this as the seed; security-scoped bookmark and Process-spawning paths are not unit-testable and should be excluded from the initial target.
- **[MEDIUM] 5 highest-value pure units to unit-test first (file:line)**
  - *Category:* testability
  - *Evidence:* 1) `CookieArgs.make(browser:file:)` DownloadEnums.swift:125 — pure, file-path-precedence branch (line 126-129); also delegated to by GalleryDlService.cookieArgs:207-209 and used by YtDlpService.buildArguments:26, so one test covers both services. 2) `YtDlpService.buildArguments(...)` YtDlpService.swift:7-80 — internal static, pure given inputs; builds the entire format-selector fallback chain (lines 30-64: audioOnly / YouTube vs non-YouTube singleFile / videoAndAudio), output template (line 24), and subtitle args (66-69). Most regression-prone surface in the app. 3) `DownloadManager.stripTrackingParams` DownloadManager.swift:700-708 with the exact-name set at line 698 — strips utm_* prefix + {si,s,ref,fbclid,gclid,...}. 4) `FxTwitterService.sanitize` FxTwitterService.swift:125-130 — pure string sanitizer (/ -> _, newline collapse, trim). 5) Filename-stem cleanup regexes in YtDlpService.parseLine:124-143 — strips `_\d+`, ` \[\d+\]`, and `\.f\d+` suffixes; currently embedded in a mutating parser.
  - *Recommendation:* Severity stays medium: a ~3700-line app with zero tests, where the most regression-prone surface is pure and trivially testable, and the format-selector even carries a known yt-dlp bug workaround (YtDlpService.swift:21-22) that begs for a pinned test. Concretely: (1) Add a .testTarget to Package.swift. (2) Start with the two already-internal statics — CookieArgs.make (DownloadEnums.swift:125; assert file-path overrides browser, and .none yields []) and YtDlpService.buildArguments (YtDlpService.swift:7; snapshot the four format-selector branches for YouTube vs non-YouTube, the heightFilter interpolation, the subtitle gate at line 66, and the playlist/output-template suffix). These need zero refactor. (3) For stripTrackingParams (private func, line 700) and FxTwitterService.sanitize (private static, line 125): they are NOT reachable via @testable import as-is — either widen to internal or test through public callers. (4) For the parseLine regex strips (124-143): extract a pure `cleanTitleStem(_:isImage:) -> String` helper (mirroring GalleryDlService's existing stem-cleanup at lines 200-204, which already factors a similar loop) so the three suffix-strip rules can be unit-tested without driving the @MainActor parser.
- **[LOW] Two highest-regression units are `private`, so unreachable even with @testable import**
  - *Category:* testability
  - *Evidence:* DownloadManager.swift:700 declares `private func stripTrackingParams` and FxTwitterService.swift:125 declares `private static func sanitize`. `@testable import` only widens `internal` to the test module — it does NOT expose `private`/`fileprivate`. As written these two cannot be unit-tested without changing source.
  - *Recommendation:* Downgrade to low: this is a testability-ergonomics nit, not dead code. Both functions are pure and are excellent first-test candidates, but the real prerequisite is the missing test target — add a `.testTarget` to Package.swift and a Tests/ dir first; the `private` keyword is only a secondary obstacle. Then for each: (a) `FxTwitterService.sanitize` is already `static` and side-effect-free — just change `private static` to `static` (or `internal static`) and assert the path/newline/trim rules. (b) `DownloadManager.stripTrackingParams` uses no instance state (it only reads the static `Self.trackingParamsExact`), so promote it to `internal static func` — this avoids instantiating the @MainActor DownloadManager just to test string logic, which is the bigger ergonomics win than merely dropping `private`. Alternatively, if you want to keep them `private`, they can still be covered indirectly via the public `addDownload(urlString:)` and FxTwitterService's public fetch path, but that drags in MainActor/network side effects and is not worth it. Net: low-priority cleanup; do it opportunistically when the first test target lands.
- **[LOW] CI compiles but never runs tests; no `swift test` step in any workflow**
  - *Category:* ci-health
  - *Evidence:* .github/workflows/build.yml runs only `swift build` (line 20) and `swift build -c release` (line 23). `grep -rn 'swift test' .github/` returns nothing. build.yml DOES gate PRs (build.yml:6-7 `pull_request: branches: [main]`) and pushes (lines 4-5), so compilation is enforced — but there is no test execution and no coverage step anywhere.
  - *Recommendation:* This is a real but low-impact process gap, not a CI bug: with no test target there is nothing for CI to run today. Treat it as a follow-up to the broader 'no tests exist' work. Concretely: (1) add a `.testTarget(name: "XDownloaderTests", dependencies: ["XDownloader"], path: "Tests/XDownloaderTests")` to Package.swift and create at least one test (e.g. cover the new CookieArgs/CookieFileScope logic); (2) then add a `- name: Test\n  run: swift test` step to .github/workflows/build.yml after the build steps (~line 23) so PRs to main are gated on tests, not just compilation. Until a test target lands, adding the step is premature.
- **[LOW] No lint or format step (swiftformat/swiftlint) configured or run in CI**
  - *Category:* ci-health
  - *Evidence:* No `.swiftformat`, `.swiftlint.yml`, or `.swift-format` config files exist (ls returned none; find for *swiftformat*/*swiftlint* outside .build returned nothing). `grep -rinE 'swiftlint|swiftformat|swift-format'` across .github/ returns nothing — no workflow invokes a formatter or linter.
  - *Recommendation:* Keep this low — it is a style/CI-hygiene gap, not dead code or a correctness bug, and it ranks below the missing-test-target gap. Concrete cheap fix: the toolchain already pins Xcode 16 (build.yml line 17), which bundles swift-format, so add a no-extra-dependency lint job to .github/workflows/build.yml, e.g. a step `swift format lint --strict --recursive Sources` (and optionally a committed .swift-format config at repo root). This enforces consistent style on PRs without installing SwiftLint/SwiftFormat. Prioritize after adding a test target.
- **[LOW] CI never exercises the app-bundle / release path until a tag is pushed**
  - *Category:* ci-health
  - *Evidence:* The `.app` bundling (`./build.sh release`, release.yml:53) and Info.plist version stamping via PlistBuddy (release.yml:38-44) run ONLY on tag push (release.yml:5-7 `tags: v[0-9]*`). build.yml on PR/push runs `swift build` but never `build.sh`, so a break in build.sh or the Info.plist keys (CFBundleShortVersionString/CFBundleVersion) surfaces only at release time. The referenced files all exist and are intact (build.sh present, Resources/Info.plist present), so nothing is currently broken — this is a coverage gap, not a broken reference.
  - *Recommendation:* Add a non-publishing bundle-smoke step to build.yml (which already runs `swift build -c release`) that runs `./build.sh release` and then asserts the bundle is well-formed, e.g.: verify `XDownloader.app/Contents/MacOS/XDownloader` exists and `/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' -c 'Print :CFBundleVersion' XDownloader.app/Contents/Info.plist` succeeds. Important caveat the bundling-only fix misses: build.yml's `actions/checkout@v4` is a shallow checkout with no tags, so build.sh's `git describe --tags --long` block is silently skipped — running build.sh alone does NOT exercise version stamping. To also cover the release.yml PlistBuddy path, either set `fetch-depth: 0` on checkout (so build.sh stamps) or add an explicit PlistBuddy `Set` smoke against Resources/Info.plist asserting both CFBundle* keys are settable. This is low priority: build.sh is already run locally on essentially every rebuild, and a release-time failure is loud and recoverable (delete tag, fix, re-tag), so it can be folded into the next CI touch-up rather than handled urgently.
- **[LOW] release.yml example comment uses 2-segment version, contradicting the project's 3-segment SemVer convention**
  - *Category:* ci-health
  - *Evidence:* release.yml:3 comment reads `# Trigger by pushing a version tag: git tag v1.1 && git push origin v1.1`. The user's stated convention is strict 3-segment SemVer (e.g. v1.4.2 — MINOR for features, PATCH for fixes). The tag glob `v[0-9]*` (release.yml:7) would still match a 2-segment `v1.1`, so the doc example actively encourages a non-conforming tag.
  - *Recommendation:* Update the comment at release.yml:3 to a 3-segment example, e.g. `# Trigger by pushing a version tag: git tag v1.5.0 && git push origin v1.5.0`. Optionally enforce the convention at the source by tightening the trigger glob at release.yml:7 from `'v[0-9]*'` to a 3-segment pattern (`'v[0-9]+.[0-9]+.[0-9]+'`) so a malformed 2-segment tag cannot start a release that stamps a non-conforming version into Info.plist.

### Dimension: code-smells (10)

- **[MEDIUM] DownloadManager.swift is a 716-line god object mixing 7 distinct concerns**
  - *Category:* God object / structural
  - *Evidence:* Sources/XDownloader/ViewModels/DownloadManager.swift is 716 lines (largest non-view file). Concretely separable responsibilities, each with its own state + methods: (1) Cookie/security-scoped-bookmark management — state at lines 13-17 (cookiesFilePath, cookiesFileBookmarkData, _bookmarkForDeinit, grantedCookiesURL, cookieScope) + deinit 68-76 + methods 214-265; (2) Settings persistence — saveSettings/loadSettings 269-320; (3) Queue management — add/retry/remove/pause/resume/clearCompleted 82-198; (4) Download+fallback orchestration — runDownload/runGalleryDlFallback/runFxTwitterFallback 322-534; (5) History admin — recordHistory/writeHistoryEntry/import/clear 536-633; (6) Queue persistence (JSON) — queueFileURL/saveQueue/loadQueue 649-696; (7) URL tracking-param stripping 698-708. The cookie block (1) and queue-persistence block (6) are self-contained and the most extract-worthy (e.g. a CookieAccessManager type and a QueueStore type).
  - *Recommendation:* Prioritize extracting the cookie/security-scoped-bookmark concern (state 13-17, deinit 68-76, methods 214-265) into a dedicated CookieAccessManager. This is the highest-value extraction not only because sandbox bookmark lifecycle is the most error-prone code, but because it is CURRENTLY mid-refactor and duplicated: DownloadManager.swift has uncommitted modifications, and the same cookie work is also being done on branch fix/cookies-scope-grant (commit f1f3576) plus the recently-added CookieFileScope.swift. Consolidating all bookmark resolve/begin/stop/deinit logic behind one type would eliminate that scatter and the duplicate effort. Second, extract the JSON queue persistence (queueFileURL/saveQueue/loadQueue, 649-696) into a QueueStore, directly mirroring the existing HistoryStore (Services/HistoryStore.swift) — a clean, low-risk move of ~48 self-contained lines. Together these remove ~120 lines and isolate the two most self-contained concerns. Do NOT attempt to extract the download/fallback orchestration (322-534) — that is the manager's legitimate core responsibility. Since there is no test target, perform these extractions as small, separately-committed, behavior-preserving refactors on a feature branch (per the project's branch-and-worktree conventions, which the in-progress cookie work already violated by being committed directly to main).
- **[MEDIUM] Confused security-scoped cookie lifecycle: 5 overlapping state vars + stop() calls that can never pair with the start()**
  - *Category:* Structural mess / lifecycle
  - *Evidence:* After the uncommitted refactor, startAccessingSecurityScopedResource is ONLY called inside CookieFileScope.begin (CookieFileScope.swift:17,20) on the grantedCookiesURL object. But three other methods resolve a BRAND-NEW URL from the bookmark data and call stopAccessing on that fresh object, which was never started: stopAccessingCookiesFile() (DownloadManager.swift:258-265, invoked by setCookiesFile:232 and clearCookiesFile:247) and deinit (68-76 via _bookmarkForDeinit). These stops are unbalanced no-ops. Meanwhile the same file is tracked by FIVE parallel members — cookiesFilePath, cookiesFileBookmarkData, _bookmarkForDeinit, grantedCookiesURL, cookieScope (lines 13-17) — and loadSettings resolves the bookmark twice in a row (303-305 sets grantedCookiesURL, then 319 calls beginAccessingCookiesFile() which resolves again and re-sets it).
  - *Recommendation:* Treat as cleanup of confusing lifecycle, not a runtime bug. (1) Collapse the 5 members to 2: keep cookiesFileBookmarkData (persisted) and the cookieScope manager; drop _bookmarkForDeinit, grantedCookiesURL as long-lived state, and let cookiesFilePath remain only as a display string. (2) Make beginAccessingCookiesFile() resolve the bookmark and RETURN the resolved URL (not store it); pass that URL straight into withScope at the two call sites (:344,:501) so start (CookieFileScope:17) and stop (CookieFileScope:30, via defer) remain the only paired start/stop. (3) Delete the unbalanced no-op stops: stopAccessingCookiesFile()'s fresh-resolve-and-stop (DM:258-265), clearCookiesFile's grantedCookiesURL stop (DM:248-250), and deinit's fresh-resolve-and-stop (DM:68-76) — none of these have a matching start, and the per-download withScope already guarantees balanced lifetime. (4) Remove the redundant second resolve in loadSettings (DM:319) since :303-305 already resolved, or drop :303-305 and keep only :319 — not both. After this, setCookiesFile/clearCookiesFile just swap cookiesFileBookmarkData and call saveSettings(). Optionally add a 2-3 line comment documenting that the ONLY grant lifetime is the per-item withScope.
- **[LOW] Media-extension literal lists duplicated across 6 sites with DIVERGENT contents (no single source of truth)**
  - *Category:* Duplication
  - *Evidence:* Hardcoded image/video extension arrays appear in: YtDlpService.swift:111 ['jpg','jpeg','png','webp'] (notably MISSING gif/avif), FxTwitterService.swift:12, GalleryDlService.swift:51, GalleryDlService.swift:140 (knownMedia), GalleryDlService.swift:143-144, and ContentView.swift:431. The sets disagree: GalleryDlService:52 videoExts includes 'mkv','m4v' but GalleryDlService:140 knownMedia does not. So the same concept ("is this an image/video file") is encoded 6+ times inconsistently.
  - *Recommendation:* Add a single shared namespace, e.g. `enum MediaExtensions { static let image: Set<String> = ["jpg","jpeg","png","webp","gif","avif"]; static let video: Set<String> = ["mp4","mov","webm","mkv","m4v"]; static let audio: Set<String> = ["m4a","mp3","aac","ogg","opus","weba"]; static let all = image.union(video).union(audio) }`. Replace all literal arrays: GalleryDlService.swift:51/52 -> MediaExtensions.image/.video; :140 knownMedia -> MediaExtensions.all (this auto-adds mkv/m4v, fixing divergence 2); :143/:144 -> .image/.video; ContentView.swift:431 -> .image; FxTwitterService.swift:12 -> .image; YtDlpService.swift:111 -> MediaExtensions.image.contains(ext) (fixing the gif/avif gap). Since there is no test target, this is low-risk find/replace; pair it with a quick manual smoke run of an image-gallery download. Treat as cleanup, not an urgent bug fix.
- **[LOW] Dead/unreachable branch: GalleryDlService.parseLine handles mkv/m4v that its own guard already rejected**
  - *Category:* Dead code
  - *Evidence:* GalleryDlService.swift:140 guard rejects any ext not in knownMedia = [jpg,jpeg,png,webp,gif,avif,mp4,mov,webm,m4a,mp3] (no mkv/m4v). Line 144 then tests isVideo = [...,'mkv','m4v'].contains(ext) — the mkv/m4v entries are unreachable because the guard at 141 already returned for them. Additionally m4a/mp3 pass the guard but match neither isImage (143) nor isVideo (144), so an audio file sets outputPath/status but increments no count.
  - *Recommendation:* Two distinct, low-impact issues in the same block. (1) Remove the unreachable "mkv","m4v" from the isVideo set on line 144 — the guard on line 141 already returns for those exts, so they can never match. (2) Decide intent for m4a/mp3, which currently pass the guard but are counted as neither image nor video: either remove them from knownMedia on line 140 (if audio-only output should be ignored — fine for an X downloader, since X yields images + mp4, not standalone audio), or, if they should count, add a branch/audio handling so the file isn't recorded with no count and no mediaCategory. Note there is no existing shared MediaExtensions type to centralize on; introducing one is optional polish, not required. Net: align the three literal lists (140/143/144) so every ext that passes the guard is classified, and drop the dead mkv/m4v.
- **[LOW] Image+video → mediaCategory derivation block copy-pasted 4 times**
  - *Category:* Duplication
  - *Evidence:* The identical 5-line 'hasImg/hasVid → .mixed/.image/.video' derivation appears at YtDlpService.swift:148-152, GalleryDlService.swift:93-97, GalleryDlService.swift:155-157, and (re-expressed) FxTwitterService.swift:90-92. Confirmed via grep: 4 occurrences of 'item.mediaCategory = .mixed'.
  - *Recommendation:* Add a single mutating helper on DownloadItem, e.g. `mutating func recomputeMediaCategory()` that reads self.imageCount/self.videoCount and sets mediaCategory (hasImg && hasVid -> .mixed; hasImg -> .image; hasVid -> .video). Replace the three identical blocks at YtDlpService.swift:148-152, GalleryDlService.swift:93-97, and GalleryDlService.swift:153-157 with a call to it. Before folding in FxTwitterService.swift:90-92, decide the both-counts-zero semantics deliberately: the helper should leave mediaCategory unchanged (matching the 3 majority sites) rather than FxTwitter's current default-to-.image, OR keep FxTwitter's fallback explicit at the call site — pick one and document it, since the current divergence is a latent inconsistency. Low priority; bundle it into the in-flight CookieArgs/service refactor touching these same files rather than a standalone change.
- **[LOW] GalleryDlService.cookieArgs is now a redundant pass-through wrapper after the CookieArgs dedup**
  - *Category:* Dead code / inconsistency
  - *Evidence:* The uncommitted diff reduced GalleryDlService.swift:207-209 cookieArgs to `return CookieArgs.make(browser: browser, file: cookiesFile)` — a pure forwarding wrapper. YtDlpService.swift:26 already calls CookieArgs.make(...) directly. So one service keeps a needless private indirection the other doesn't.
  - *Recommendation:* Severity correctly stays low (cosmetic dead-code/inconsistency, zero functional impact). Inline the single call site: change GalleryDlService.swift:19 from `var args = cookieArgs(cookieBrowser, cookiesFile: cookiesFile)` to `var args = CookieArgs.make(browser: cookieBrowser, file: cookiesFile)`, then delete the private wrapper at GalleryDlService.swift:207-209. That makes GalleryDlService match YtDlpService.swift:26 exactly and leaves no other callers (no test target references it). Optional, do it alongside the in-flight CookieArgs refactor commit rather than as a standalone change.
- **[LOW] Stale comment references a 'dry-run' path that does not exist in the code**
  - *Category:* Stale comments
  - *Evidence:* GalleryDlService.swift:66 comment: 'gallery-dl exited 0 but neither new files appeared nor dry-run could resolve a path'. grep for 'dry-run|--simulate|--get-url' across Sources/ matches ONLY this comment — there is no dry-run/simulate invocation anywhere in run(). The comment describes removed/never-implemented behavior.
  - *Recommendation:* Edit the comment at GalleryDlService.swift:66-67: drop the 'nor dry-run could resolve a path' clause so it reads '// gallery-dl exited 0 but no new files appeared — the tweet's media is genuinely unreachable...'. This matches the actual guard (item.outputPath != nil at line 73), which is set only from the filesystem diff. No code change is needed; the guard logic is correct.
- **[LOW] Duplicated near-identical comment sentences about cookies.txt export in runDownload**
  - *Category:* Stale comments
  - *Evidence:* DownloadManager.swift:373-374 contains two back-to-back sentences saying essentially the same thing: 'Use cookies.txt export for X NSFW videos.' then 'Use a cookies.txt export (Settings) for X adult/NSFW media that browser cookies can't reach.' One is redundant. (The same advice is also re-stated in the diff at GalleryDlService.swift:69/77.)
  - *Recommendation:* In Sources/XDownloader/ViewModels/DownloadManager.swift, on line 373 delete only the appended sentence "Use cookies.txt export for X NSFW videos." so the line reads "// can't reach, and sensitive content the current cookies don't unlock.", and keep the fuller sentence on line 374 ("// Use a cookies.txt export (Settings) for X adult/NSFW media that browser cookies can't reach."). Leave GalleryDlService.swift:69/77 as-is — line 77 is a user-facing error string, not a redundant comment. Comment-only cleanup, no functional impact.
- **[LOW] Title-stem stripping logic duplicated and divergent across parsers**
  - *Category:* Duplication
  - *Evidence:* Filename-stem cleanup is implemented separately in YtDlpService.swift:124-143 (strips _\d+$, " [\d+]$", ".f\d+$" inline) and GalleryDlService.swift:197-205 (displayTitle strips " #\d+$", " [\d{10,}]$", _\d+$). They share the _\d+$ rule but otherwise differ, so the displayed title for the same tweet can differ depending on which backend won.
  - *Recommendation:* Optional cleanup, not a bug. If consolidating, add one helper (e.g. DownloadItem.cleanStem(forPath:) or a TitleFormatter) that applies the UNION of suffix rules in a fixed order: ".f\d+$", " [\d+]$", " [\d{10,}]$", " #\d+$", then "_\d+$". Because yt-dlp never emits " #N"/" [tweet_id]" and gallery-dl never emits ".f136"/" [01]", applying all patterns to either backend's filename is safe and would make the displayed title stable regardless of which backend wins. One behavioral nuance to preserve or consciously change: yt-dlp currently strips "_\d+$" only when isImage, whereas gallery-dl always does — a naive merge that unconditionally strips "_\d+$" could clip a legitimate trailing "_123" from a non-image title, so decide that explicitly. Given it is purely cosmetic and both paths already work, this is a low-priority DRY tidy-up, fine to defer.
- **[LOW] Non-idiomatic underscore-prefixed property name _bookmarkForDeinit**
  - *Category:* Naming
  - *Evidence:* DownloadManager.swift:15 declares `private var _bookmarkForDeinit: Data?`. Swift convention reserves the leading-underscore style for synthesized/backing storage; here it is an ordinary stored property whose purpose (a non-isolated deinit copy) would read better as a descriptive name.
  - *Recommendation:* Rename `_bookmarkForDeinit` to a descriptive, underscore-free name such as `deinitBookmarkData`. Better: note that it is a redundant mirror of `cookiesFileBookmarkData` (always assigned/cleared together at lines 233/234, 252/253, 259/264, 299/300) and the stated "non-isolated copy" rationale does not hold — both are @MainActor-isolated stored properties — so the cleaner fix is to delete `_bookmarkForDeinit` entirely and have deinit/stopAccessingCookiesFile operate on the single `cookiesFileBookmarkData` source of truth, removing one of two parallel properties that must be kept in sync.
